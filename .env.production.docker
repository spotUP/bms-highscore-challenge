# ── RetroRanks Docker Production Environment ──
# Copy this file to .env and fill in the values before running docker compose.

# ── Secrets (generate unique values!) ──
# openssl rand -base64 32
POSTGRES_PASSWORD=CHANGE_ME_generate_a_strong_password
JWT_SECRET=CHANGE_ME_generate_a_strong_secret

# ── Cloudflare R2 ──
VITE_CLOUDFLARE_R2_DOMAIN=pub-1a84b69be18749cc982661f2fd3478b2.r2.dev

# ── Build-time variables (baked into frontend JS) ──
VITE_API_URL=https://retroranks.com
VITE_WS_URL=wss://retroranks.com
VITE_MEDIA_BASE_URL=https://retroranks.com

# ── Optional API keys ──
VITE_RAWG_API_KEY=
VITE_IGDB_CLIENT_ID=
VITE_IGDB_ACCESS_TOKEN=

# ── Public base URL (used by server for media URLs) ──
PUBLIC_BASE_URL=https://retroranks.com
